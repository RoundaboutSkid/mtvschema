name = "medeltidsveckan-fav"
main = "src/worker.mjs"
compatibility_date = "2024-11-01"

# Key-value store that holds each browser's favourite ids.
# Create it once with:
#     npx wrangler kv namespace create MV_KV
# then paste the printed id below (replace REPLACE_WITH_KV_ID).
[[kv_namespaces]]
binding = "MV_KV"
id = "ea57af6db204498f971cbfe68c46e542"
